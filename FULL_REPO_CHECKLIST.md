# Full Repo Checklist - Both Person A + B - Necessary Folders Only

This is the minimal necessary structure for upload (no fake reports).

## Root Files (Necessary)
- README.md - Main GitHub README (full project)
- .gitignore - VLSI ignores (vcd, runs/, gds.gz, sky130.lib, etc.)
- PROJECT_STATE.md - PASTE AT TOP OF EVERY NEW CHAT
- TEAM_SPLIT.md - 2-person split overview
- PERSON_A_TASKS.md - RTL/Synth/STA tasks
- PERSON_B_TASKS.md - PNR/Virtuoso/FPGA tasks
- MASTER_RETRIEVAL_NOTE.md - Original retrieval note

Optional but helpful (kept):
- OPEN_SOURCE_FLOW.md - Yosys/OpenSTA/OpenLane+Virtuoso flow
- INTERVIEW_CHEATSHEET.md - Numbers to memorize
- PERSON_A_HANDOVER.md - Handover template

## Necessary Folders for Both

```
rtl/                          # Person A
  crypto/ (12 files: aes_sbox, sub_bytes, shift_rows, gf_mult, mix_columns, key_expand, aes_core...)
  peripheral/ (uart_rx, tx)
  top/ (aes_soc.v)

tb/                           # Person A - NIST verification
  tb_aes_core.v
  tb_aes_soc.v

synth/                        # Person A - Yosys
  synth.tcl
  get_pdk.sh
  netlist/ (empty, will be generated: aes_soc_synth.v)

timing/                       # Person A - OpenSTA
  constraints.sdc (20ns 50MHz)
  sta.tcl
  reports/ (empty, will be generated: setup_report.txt, hold_report.txt)

pnr/                          # Person B - OpenLane 2
  config.json (DIE 0 0 1000 1000, 45% util, CLOCK_PERIOD 20)
  src/ (15 files copy of rtl for OpenLane dir::src/*.v)
  # runs/ is gitignored - will be generated

gds/                          # Person B - Final GDS will be copied here
  aes_soc.gds (from pnr/runs/.../final/gds/)

reports/                      # Person B - Final area/power/timing/DRC/LVS (empty now, fill after runs)
  (empty - Person A/B fills after runs)

docs/                         # Both - Documentation
  ARCHITECTURE.md
  NIST_VERIFICATION.md
  VIRTUOSO_GUIDE.md

virtuoso/                     # Person B - Virtuoso licensed advantage
  screenshots/ (empty now - Person B fills 4 images after Virtuoso import)

fpga/                         # Person B - FPGA Demo LAST
  basys3.xdc
  icestick.pcf

sw/                           # Person B - Python demo
  pc_demo.py

cadence/ (optional)           # If lab gives Genus/Innovus/Xcelium later
  synthesis/
  layout/
  simulation/
```

## What NOT to create (you said no exact reports)

Don't create fake:
- reports/area_report.txt with exact numbers
- reports/timing_setup.rpt with faked slack
- Any generated netlist or GDS now - those will be generated by tools later

Keep folders empty with .gitkeep so git tracks them.

## Git Setup for Full Repo (Both)

```bash
cd aes-crypto-soc
git init
git add .
git commit -m "[AES][INIT] Full repo skeleton for both A+B - necessary folders only"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/AES-Crypto-SoC.git
git push -u origin main

# Person A work
git checkout -b feature/aes-rtl-synth-timing
# ... do RTL/Synth/STA ...
git add rtl/ tb/ synth/ timing/
git commit -m "[AES][RTL+SYNTH+STA][A] Complete"
git push origin feature/aes-rtl-synth-timing

# Person B work
git checkout -b feature/aes-pnr-gds-virtuoso
# ... do PNR ...
git add pnr/ gds/ reports/ virtuoso/ fpga/
git commit -m "[AES][PNR+GDS+VIRTUOSO][B] Complete"
git push origin feature/aes-pnr-gds-virtuoso

# Final merge for upload
git checkout main
git merge feature/aes-rtl-synth-timing feature/aes-pnr-gds-virtuoso
git push origin main
```

## Current Status After Cleanup (2026-07-28)

- Necessary folders created for both A+B ✅
- No fake exact reports ✅ (only .gitkeep)
- RTL 15 files present ✅
- TB 2 files ✅
- Synth TCL + SDC + STA TCL ✅
- PNR config + src copy ✅
- FP borads xdc + SW demo ✅
- Docs ARCHITECTURE, NIST, VIRTUOSO_GUIDE ✅
- Team docs PROJECT_STATE, TEAM_SPLIT, PERSON_A/B_TASKS ✅
- Ready to upload to GitHub as full skeleton ✅

Person A can start Yosys synthesis now, Person B can start OpenLane rehearsal in parallel.
